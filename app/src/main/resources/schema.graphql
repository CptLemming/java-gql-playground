type Query {
  hello: String
  faders: [Fader]
  fader(pathType: PathType): Fader
}

type Fader {
  id: ID
  label: String
  isAccessed: Boolean
  type: PathType
}

type Subscription {
  fader(pathType: PathType): Fader
}

enum PathType {
  CHANNEL,
  MAIN,
  GROUP,
  AUX,
  TRACK
}
